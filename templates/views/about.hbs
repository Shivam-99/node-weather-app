<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    
    <title>About</title>
    <style>
      body {
        width: 700px;
        min-height: 100vh;
        margin: 0 auto;
        display: flex;
        flex-direction: column;
      }

      header {
        text-align: center;
        margin-top: 20px;
        margin-bottom: 48px;
      }

      header h1 {
        font-size: 40px;
        width: fit-content;
        margin: auto;
      }
      header .links {
        width: fit-content;
        margin: auto;
      }
      header a {
        margin-right: 16px;
        text-decoration: none;
        color: #888888;
      }

      .content{
        flex-grow: 1;
      }
      img{
        height: 300px;
      }
      footer {
        padding-top: 16px;
        color: grey;
      }
    </style>
  </head>
  <body>
      {{>header}}
      <div class="content">
          <img src="../assets/prof.jpg" alt="profile">
          <p>Create by {{name}}</p>
      </div>
      

      {{>footer}}
  </body>
</html>